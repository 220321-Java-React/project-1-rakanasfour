package com.revature;



import static org.junit.Assert.assertNotNull;
import static org.junit.Assert.assertTrue;

import org.junit.Before;
import org.junit.Test;

import com.revature.daos.GradeDAO;
import com.revature.daos.StudentDAO;
import com.revature.daos.UserDAO;
import com.revature.models.Grade;
import com.revature.models.Menu;
import com.revature.models.Student;



public class TestClass {
	/*
	
	
		static UserDAO userDAO;
		String A = "Manager";
		String B = "Manager";
		String name = "Gon";
		int math = 99;
		int english = 88;
		int science = 89;
		boolean display = true;

		
		
		
		
		@Before
		public void initialize() {
			System.out.println("In the @Before method");
			userDAO = new UserDAO();
		}
		
		@Test
		public void testLogin() {
			
			System.out.println("TESTING UserDAO");
			
			
			
			boolean result = userDAO.login(A, B); //we're assigning the result of the add() method to this result variable
			
			assertTrue(result == true); //this test will pass if result == 10.
			
		}


		
		
		@Test
		public void testGradeDAO() {
			System.out.println("In the Test grade method ");
			GradeDAO gradeDAO = new GradeDAO();
			assertNotNull(gradeDAO.getGradeByName(name));
			
		}
		
		
		

		@Test
		public void testStudent() {
			System.out.println("In test StudentDAO ");
			StudentDAO studentDAO = new StudentDAO();
			assertNotNull(studentDAO.getStudents());
		}
		
		
			
		*/	
		
		
		/*
		@Test(expected = SomeException.class)
		public void testDisplayMenu() {
		//Menu menu = new Menu();
		
		Throwable exception = assertThrows(InputMismatchException.class, () -> {
			@Override
			public void execute() throws throwable{
				Menu menu = new Menu();
				menu.
				
				
			}
			assertEquals("Please a number", exception.getMessage())
			
			
		}
		
		String expectedMessage = "Please a number";
		String actualMessage = exception.getMessage();
		assertTrue(actualMessage.contains(expectedMessage));
		}
		
		*/
		
			
			
			 
		/*
		@Before
		Grade grade = new Grade(
				//we want to use rs.get() for each column in the record	
				
				math, //rs.getInt("math"),
				 english, //rs.getInt("english"),
				science//.getInt("science")
				
				
				);
		assertEquals(g)
		Grade(math, eb)
		
*/
	
}
